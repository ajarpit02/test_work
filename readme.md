# Simple Python Repo

This is a very small repo with simple Python functions.

## Files

### `main.py`
Runs sample functions.

### `utils.py`
Contains:
- `greet(name)` → returns greeting message  
- `add_numbers(a, b)` → returns sum of two numbers

## Run

```bash
python3 main.py
